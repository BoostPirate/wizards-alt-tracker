# Wizards Alt Tracker

Made to track gp amount on your alt/mule accounts, so you don't have to log in to check.

- Tracks total coins from inventory + bank
- Optional RSN filter (only runs on configured mule names)
- Sends updates to a configurable HTTP endpoint (Discord webhook or external API)
